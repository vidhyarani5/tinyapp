const urlDatabase = {
  k6BoDs: {
    longURL: "https://www.google.ca",
    userId: "6gpd8"
  }
}; //Store the urls
const users = {};       //Stores the users

exports.urlDatabase = urlDatabase
exports.users = users
